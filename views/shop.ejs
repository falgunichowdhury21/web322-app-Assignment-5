<div class="row">
    <section class="main col-md-9">
      <div class="row">
        <div class="col-4">
          <h2>Falguni's Store</h2> 
          <img
            src="https://media.greatbigphotographyworld.com/wp-content/uploads/2023/03/resolution-featured-image.jpg"
            alt="feature Image"
            style="width:100%"
          />
        </div>
  
        <div class="col-8"><br />
          <br />
          <div class="row">
            <div class="col-12">
                "massa non ante bibendum ullamcorper. Duis cursus, diam at pretium
                aliquet, metus urna convallis erat, eget tincidunt dui augue eu
                tellus. Phasellus elit pede, malesuada vel, venenatis vel,
                faucibus id, libero. Donec consectetuer mauris id sapien. Cras
                dolor dolor, tempus non, lacinia at, iaculis quis, pede. Praesent
                eu dui. Cum sociis natoque penatibus et magnis dis parturient
                montes, nascetur ridiculus mus. Aenean eget magna. Suspendisse
                tristique neque venenatis lacus. Etiam bibendum fermentum metus.
                Aenean sed",
   
              <br /><br />
              <h4>Latest Items</h4>
                  <% if (items && items.length) { %>
                      <% items.forEach(item => { %>
                          <div class="item">
                              <strong>Title:</strong> <%= item.title %> <br />
                              <strong>Price:</strong> $<%= item.price.toFixed(2) %> <br />
                              <strong>Category:</strong> <%= item.Category ? item.Category.category : 'N/A' %> <br />
                              <strong>Published:</strong> <%= item.published ? 'Yes' : 'No' %> <br />
                              <br />
                          </div>
                      <% }); %>
                  <% } else { %>
                      <p>No items available.</p>
                  <% } %>
              </div>
          </div>
      </section>
  
      <aside class="col-md-3">
          <br /><br />
          <h4>Items</h4>
          <ul class="list-group">
              <% if (items && items.length) { %>
                  <% items.forEach(item => { %>
                      <li class="list-group-item">
                          <a href="/items/<%= item.id %>"><%= item.title %></a>
                      </li>
                  <% }); %>
              <% } else { %>
                  <li class="list-group-item">No items available.</li>
              <% } %>
          </ul>
    
          <h4>Categories</h4>
          <ul class="list-group">
              <% if (categories && categories.length) { %>
                  <% categories.forEach(category => { %>
                      <li class="list-group-item">
                          <a href="/items?category=<%= category.id %>"><%= category.category %></a>
                      </li>
                  <% }); %>
              <% } else { %>
                  <li class="list-group-item">No categories available.</li>
              <% } %>
          </ul>
      </aside>
  </div>